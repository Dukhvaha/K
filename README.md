# Telegram Movies Bot

Полнофункциональный Telegram-бот для поиска и просмотра фильмов с интеграцией Kinopoisk API и кешированием видео.

## 🚀 Возможности

- 🔍 Поиск фильмов по названию
- 🎲 Случайный фильм
- 💾 Кеширование видео через Telegram CDN
- 📊 Интеграция с Kinopoisk API
- ⭐ Избранное (в разработке)
- 🎨 Удобный UI с inline и reply клавиатурами

## 📋 Требования

- Python 3.11+ (рекомендуется 3.12)
- Telegram Bot Token
- Приватный Telegram канал (для хранения видео)
- Kinopoisk API Token (опционально, для метаданных)

## 🛠 Установка

### Локальная установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd K
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
playwright install chromium
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните `.env` файл:
```env
API_TOKEN=your_telegram_bot_token
CHANNEL_ID=your_channel_id
KINOPOISK_TOKEN=your_kinopoisk_token
DATABASE_URL=sqlite+aiosqlite:///./movies.db
```

6. Запустите бота:
```bash
python -m bot.main
```

## 📁 Структура проекта

```
K/
├── bot/                    # Основной код бота
│   ├── handlers/          # Обработчики команд
│   │   ├── start.py       # /start
│   │   ├── film.py        # /film, /search
│   │   ├── help.py        # /help
│   │   ├── random.py      # /random
│   │   └── text.py        # Обработка текста
│   ├── keyboards/         # Клавиатуры
│   ├── middlewares/       # Middleware
│   ├── file_storage.py    # Работа с файлами
│   └── main.py           # Точка входа
├── database/              # Работа с БД
│   ├── connection.py     # Подключение к БД
│   └── models.py         # Модели данных
├── services/             # Внешние сервисы
│   ├── downloader.py     # Скачивание видео
│   ├── kinopoisk_service.py  # Kinopoisk API
│   └── zona_parser_service.py # Парсер zona.plus
├── config.py             # Конфигурация
├── requirements.txt      # Зависимости
└── README.md           # Документация
```

## 🎮 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/film <название>` - Найти фильм по названию
- `/search <название>` - Альтернативный поиск
- `/random` - Получить случайный фильм
- `/help` - Справка

### Примеры

```
/film Матрица
/film Inception
/random
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательно |
|-----------|----------|-------------|
| `API_TOKEN` | Telegram Bot Token | Да |
| `CHANNEL_ID` | ID приватного канала | Да |
| `KINOPOISK_TOKEN` | Kinopoisk API Token | Нет |
| `DATABASE_URL` | URL базы данных | Да |
| `ENV` | Окружение (development/production) | Нет |
| `LOG_LEVEL` | Уровень логирования | Нет |

## 📝 Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements.txt
```

### Линтинг

```bash
ruff check .
black --check .
mypy .
```

### Тестирование

```bash
pytest tests/
```

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте правильность `API_TOKEN`
2. Убедитесь, что бот запущен
3. Проверьте логи

### Ошибки с базой данных

1. Проверьте `DATABASE_URL`
2. Убедитесь, что БД доступна
3. Проверьте права доступа

### Видео не загружается

1. Проверьте доступность `CHANNEL_ID`
2. Убедитесь, что бот - админ канала
3. Проверьте интернет-соединение

## 📄 Лицензия

MIT

## 🤝 Вклад

Приветствуются Pull Request'ы! Пожалуйста, откройте Issue для обсуждения больших изменений.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

